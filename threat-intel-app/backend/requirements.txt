fastapi>=0.136.1
uvicorn[standard]==0.30.1
langgraph==0.1.19
langchain==0.2.6
langchain-openai==0.1.14
openai>=1.32.0,<2.0.0
httpx==0.27.0
aiohttp>=3.12.0,<4.0.0
python-dotenv==1.0.1
taxii2-client==2.3.0
stix2==3.0.1
pymisp==2.4.193
pydantic>=2.9.0
python-multipart==0.0.9
aiofiles==23.2.1
mitreattack-python
sigma-cli
eml-parser
yara-python>=4.3.0
anthropic>=0.34.0
mcp
bcrypt>=4.0.0
itsdangerous>=2.0.0
# dnstwist powers the domain_permutations skill (typosquat / homoglyph
# enumeration for URL-input alerts). MIT-licensed, pure Python.
dnstwist>=20240819
# flare-capa is OPTIONAL — opt-in via RECON_ENABLE_CAPA=1. Pulls ~150 MB
# of transitive deps (vivisect, viv-utils, etc.) and spends 5-30s per
# real PE. The rules-based intel/file_capability_map.py still produces
# a MITRE technique list from PE imports without it. Comment out if
# disk / install time is constrained.
flare-capa>=8.0.0
# scikit-learn powers the round-14 ML enhancements:
#   intel/dga_classifier.py          — LogisticRegression over char-ngrams
#   intel/phishing_url_classifier.py — GradientBoosting over URL features
#   intel/semantic_search.py         — TF-IDF fallback embedder
# Trained at first call on bundled in-tree datasets (no disk persistence).
scikit-learn>=1.4.0
# sentence-transformers is OPTIONAL — when installed, semantic_search.py
# embeds with all-MiniLM-L6-v2 (~80MB) instead of the TF-IDF fallback.
# Comment out if disk / install time is constrained.
# sentence-transformers>=2.7.0
